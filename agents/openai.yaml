interface:
  display_name: "Local File Search"
  short_description: "Find local files and search document contents quickly."
  default_prompt: "Use $local-file-search to find local files by name or path, or search text inside local documents."
